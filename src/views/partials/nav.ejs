<header class="brand">
  <a href="/movies" class="brand-link">
    <span class="brand-name-wrap">
      <img src="/gc-mark.png" alt="" class="brand-mark" />
      <span class="brand-name">GCinema</span>
    </span>
  </a>
  <p class="brand-tagline">Beyond Screens, Toward Connected Cinema</p>
</header>
<nav>
  <div class="nav-left">
    <div class="breadcrumb">
      <% var crumbs = (typeof breadcrumb !== 'undefined' && breadcrumb) ? breadcrumb : ['영화 목록']; %>
      <a href="/movies"><%= crumbs[0] %></a>
      <% for (var i = 1; i < crumbs.length; i++) { %>
        <span class="breadcrumb-sep">&gt;</span>
        <span class="breadcrumb-current"><%= crumbs[i] %></span>
      <% } %>
    </div>
  </div>
  <div class="nav-right">
    <% if (userId) { %>
      <a href="/bookings">마이페이지</a>
      <form action="/logout" method="post" style="margin:0">
        <button type="submit" style="padding:2px 8px">로그아웃</button>
      </form>
    <% } else { %>
      <a href="/login">로그인</a>
    <% } %>
    <a href="/support">고객센터</a>
  </div>
</nav>
